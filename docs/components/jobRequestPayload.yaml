JobRequestPayload:
  type: object
  required:
  - project
  properties:
    project:
      type: string
    identifier:
      type: string
    desc:
      type: string
    build:
      $ref: "/JobRequestPayload.yaml#/Build"

Build:
  type: object
  required:
  - aut
  properties:
    aut:
      type: string
    test:
      type: string 
