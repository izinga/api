jobRequestPayload:
  type: object
  required:
  - project
  properties:
    project:
      type: string
    build:
      type: object
        properties:
          aut:
            type: string
          test:
            type: string